import 'package:flutter/material.dart';

class CenterCircularProgressIndication extends StatelessWidget {
  const CenterCircularProgressIndication({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
